import 'package:flutter/material.dart';

class BusinessServiceView extends StatelessWidget {
  const BusinessServiceView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Pagina 3'),
    );
  }
}
